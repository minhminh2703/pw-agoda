{
  "name": "pw-agoda",
  "version": "1.0.0",
  "description": "Playwright tests with Allure reporting",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test && npm run report:generate && npm run report:show",
    "test:headed": "npx playwright test --headed && npm run report:generate && npm run report:show",
    "test:flights": "npx playwright test tests/flights.spec.ts && npm run report:generate && npm run report:show",
    "test:navigation": "npx playwright test tests/navigation.spec.ts && npm run report:generate && npm run report:show",
    "test:ui": "npx playwright test --ui",
    "test:only": "npx playwright test",
    "report:generate": "rm -rf allure-report && allure generate -o allure-report allure-results",
    "report:open": "allure open allure-report",
    "report:show": "echo '\n✅ Test execution completed!\n📊 Allure report generated at: allure-report/index.html\n🔗 To view the report, run: npm run report:open\n'",
    "report:clean": "rm -rf allure-results allure-report && mkdir -p allure-results"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.2",
    "allure": "^3.0.0",
    "allure-playwright": "^3.0.0"
  },
  "overrides": {
    "underscore": ">=1.12.1",
    "fast-xml-parser": ">=5.3.4"
  },
  "dependencies": {
    "dotenv": "^17.2.4"
  }
}
